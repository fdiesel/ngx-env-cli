{
  "name": "ngx-env-cli",
  "version": "1.0.2",
  "description": "CLI for managing environment variables in angular",
  "repository": {
    "type": "git",
    "url": "https://github.com/fdiesel/ngx-env-cli"
  },
  "main": "./src/index.ts",
  "bin": {
    "ngx-env": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc && cp package.json dist/ && cp package-lock.json dist/ && cp README.md dist/ && cp LICENSE dist/",
    "test": "jest"
  },
  "author": "fdiesel",
  "license": "ISC",
  "dependencies": {
    "case": "^1.6.3",
    "commander": "^11.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "@types/node": "^20.4.5",
    "jest": "^29.6.2",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
